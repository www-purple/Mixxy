language: java
sudo: false
jdk:
  - openjdk7
  - oraclejdk7

before_deploy:
  - mvn appengine:update

deploy:
  provider: gae
  keyfile: mixxy-secret.json
  project: mixxy
  config: src/main/webapp/WEB-INF/appengine-web.xml
  on:
    jdk: oraclejdk7
cache:
  directories:
    - $HOME/.m2